# Builder Roles

Use `cw-agent-brainstormer` for open possibilities, `cw-agent-outliner` for structure after a direction is chosen, `cw-agent-writer` for prose, and `cw-agent-style-creator` for style guides.

Do not use outlining to prematurely close open creative questions. Do not use drafting to hide unresolved canon problems.

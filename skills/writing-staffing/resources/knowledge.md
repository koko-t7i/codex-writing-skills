# Knowledge Roles

Use `cw-agent-session-miner` for recovering decisions from notes, `cw-agent-chronicler` for extracting facts from chapters, and `cw-agent-graph-maintainer` for relationship maps and links.

Knowledge work should preserve confirmed facts, rejected alternatives, and unresolved questions without turning speculation into canon.

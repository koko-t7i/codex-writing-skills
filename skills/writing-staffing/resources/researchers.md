# Research Roles

Use `cw-agent-researcher` for external sources and `cw-agent-explorer` for project-local files.

Research is useful when a story question depends on real-world detail, genre conventions, historical constraints, technical facts, or existing project context. For broad questions, split the work by topic only when the user explicitly asks for parallel delegation.
